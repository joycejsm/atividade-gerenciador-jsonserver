{
  "name": "full-stack-atividades",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node setupPublicDir.js && json-server --watch db.json --port $PORT",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
